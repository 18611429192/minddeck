# MindDeck V9.5.2 RC 热修

这版只处理演示可靠性和母版编辑回归。

## 母版编辑

修复侧栏折叠状态串到母版编辑器的问题。

现在：

- 进入母版时会清理上一轮页面编辑残留的 `panel-collapsed`
- 进入母版时会重新同步背景色、背景图片适配、目录显示和目录位置
- 没有选中母版元素时，右侧明确显示“母版背景”设置
- 选中母版元素时，右侧立即切换到该元素属性
- 点击母版空白区域后，重新回到“母版背景”设置
- 母版编辑不再继承演示模式残留状态
- 母版模式增加轻量紫色状态提示，避免误以为正在编辑普通页面

## 演示可靠性

- `Esc` 现在无条件执行退出演示，不依赖 fullscreenchange
- 浏览器拒绝 Fullscreen 时，仍然进入可用的窗口演示，并明确提示 `Esc` 退出
- 退出演示会强制清理 `presentation-mode` 和演示容器状态
- 进入页面/母版编辑器时也会清理可能残留的演示状态

## 回归建议

### 母版
1. 打开任意节点页面编辑
2. 收起右侧属性栏
3. 切到母版
4. 确认“母版背景”设置立即出现并可修改
5. 新增母版文字
6. 点击文字，确认元素属性出现
7. 点击母版空白处，确认重新显示母版背景设置
8. 返回当前页，确认母版固定元素仍显示

### 演示
1. 点击开始演示
2. 测试 `Esc` 退出
3. 再进入演示，测试 `↑ ↓ ← → / PageUp / PageDown / Space`
4. 测试鼠标滚轮
5. 浏览器不进入 Fullscreen 时，也应能正常演示和 Esc 退出
