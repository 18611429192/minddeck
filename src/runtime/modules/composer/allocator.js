import { composerHashString } from './base.js';
export function createAllocationState(){return {templateUsage:new Map(),familyUsage:new Map(),previousFamily:null,previousTemplate:null}}
function composerTie(seed,slideId,templateId){return parseInt(composerHashString(`${seed}:${slideId}:${templateId}`),36)%1000/100000}
export function allocateTemplates(slides,candidateMatrix,options={}){const state=createAllocationState(),seed=options.seed||'minddeck',maxReuse=options.maxTemplateReuse??2,adjacentFamilyPenalty=options.adjacentFamilyPenalty??40,templateReusePenalty=options.templateReusePenalty??30,familyReusePenalty=options.familyReusePenalty??12,assignments=[];for(let index=0;index<(slides||[]).length;index++){const slide=slides[index]||{},candidates=(candidateMatrix?.[index]||[]).map(candidate=>({...candidate}));if(!candidates.length){assignments.push({slideId:slide.id,templateId:null,family:null,score:-Infinity,alternatives:[],reason:'NO_TEMPLATE_CANDIDATE'});continue}const scored=candidates.map(candidate=>{const templateUses=state.templateUsage.get(candidate.templateId)||0,familyUses=state.familyUsage.get(candidate.family)||0;let score=Number(candidate.score)||0;if(candidate.templateId===state.previousTemplate)score-=templateReusePenalty+20;if(candidate.family===state.previousFamily)score-=adjacentFamilyPenalty;score-=templateUses*templateReusePenalty+familyUses*familyReusePenalty;if(templateUses>=maxReuse)score-=120*(templateUses-maxReuse+1);score+=composerTie(seed,slide.id||index,candidate.templateId);return {...candidate,allocationScore:score}}).sort((a,b)=>b.allocationScore-a.allocationScore||a.templateId.localeCompare(b.templateId)),picked=scored[0];state.templateUsage.set(picked.templateId,(state.templateUsage.get(picked.templateId)||0)+1);state.familyUsage.set(picked.family,(state.familyUsage.get(picked.family)||0)+1);state.previousTemplate=picked.templateId;state.previousFamily=picked.family;assignments.push({slideId:slide.id,templateId:picked.templateId,family:picked.family,score:picked.allocationScore,alternatives:scored.slice(1,4).map(item=>item.templateId),reason:'ALLOCATED'})}return assignments}
