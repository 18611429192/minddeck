import { composerClean } from './base.js';
import { normalizeSlideContent, inferSlideRole } from './schema.js';
export function parseComposerMarkdown(source){const lines=String(source??'').replace(/\r/g,'').split('\n');let title='',subtitle='';const specs=[],stack=[];let current=null,headingSeen=false;const pushNode=(level,value)=>{const item={title:composerClean(value),text:'',points:[],children:[],level:Math.max(1,level)};while(stack.length&&stack.at(-1).level>=item.level)stack.pop();if(stack.length)stack.at(-1).children.push(item);else specs.push(item);stack.push(item);current=item;return item};for(const raw of lines){const line=raw.trimEnd();if(!line.trim())continue;const heading=line.match(/^\s*(#{1,6})\s+(.+)$/);if(heading){headingSeen=true;const level=heading[1].length,value=heading[2];if(level===1&&!title){title=composerClean(value);current=null;stack.length=0}else pushNode(Math.max(1,level-1),value);continue}const quote=line.match(/^\s*>\s*(.+)$/);if(quote&&!current){subtitle=composerClean([subtitle,quote[1]].filter(Boolean).join(' '));continue}const bullet=line.match(/^\s*(?:[-*+•]|\d+[.)])\s+(.+)$/);if(bullet){if(!current)current=pushNode(1,'核心内容');current.points.push(composerClean(bullet[1]));continue}if(!title&&!headingSeen){title=composerClean(line);continue}if(current)current.text=composerClean([current.text,line.trim()].filter(Boolean).join(' '));else subtitle=composerClean([subtitle,line.trim()].filter(Boolean).join(' '))}return {title:title||'未命名演示',subtitle,specs,headingSeen}}
export function parseComposerIndented(source){const lines=String(source??'').replace(/\r/g,'').split('\n').filter(line=>line.trim());if(!lines.length)return {title:'未命名演示',subtitle:'',specs:[]};const first=composerClean(lines.shift().replace(/^\s*(?:[-*+•]|\d+[.)])\s+/,'')),specs=[],stack=[];for(const raw of lines){const indent=(raw.match(/^\s*/)?.[0]||'').replace(/\t/g,'  ').length,level=Math.max(1,Math.floor(indent/2)+1),value=composerClean(raw.replace(/^\s*(?:[-*+•]|\d+[.)])\s+/,''));if(!value)continue;const item={title:value,text:'',points:[],children:[],level};while(stack.length&&stack.at(-1).level>=level)stack.pop();if(stack.length)stack.at(-1).children.push(item);else specs.push(item);stack.push(item)}return {title:first||'未命名演示',subtitle:'',specs}}
export function parseComposerOutline(source){const markdown=parseComposerMarkdown(source);if(markdown.headingSeen||markdown.specs.length)return markdown;return parseComposerIndented(source)}
export function composerSpecContent(spec={}){return normalizeSlideContent({title:spec.title,summary:spec.text,items:(spec.points||[]).map(label=>({label}))})}
export function composerSpecRole(spec={},context={}){return inferSlideRole(composerSpecContent(spec),null,context)}
