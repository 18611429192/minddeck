  // Unified V10 redesign flow: DesignIntent is the constraint input; A/B/C are concrete outcomes.
  const redesignDraftV10=new Map();
  function redesignIntentV10(node){return redesignDraftV10.has(node.id)?redesignDraftV10.get(node.id):ComposerV10Inspector.readDesignIntent(node)}
  function redesignCandidatesV10(node,intent){try{return ComposerV10Inspector.resolveDesignIntent({node,intent,theme:node.deckTheme||data.deckTheme,density:intent.density||node.deckDensity||data.deckDensity,limit:3})}catch(err){return {error:err,candidates:[]}}}
  function redesignCandidateHtmlV10(resolution){const labels=['A 推荐','B 备选','C 备选'];return (resolution.candidates||[]).slice(0,3).map((candidate,index)=>`<button type="button" class="v99-smart-template" data-redesign-template="${esc(candidate.templateId)}"><span class="rank">${labels[index]}</span><b>${esc(candidate.label||candidate.templateId)}</b><small>${esc((candidate.reasons||[]).join(' · ')||'符合当前设计意图')}</small><code>${esc(candidate.templateId)} · ${esc(candidate.family||'')}</code></button>`).join('')||`<div class="v10-inspector-warning">${esc(resolution.error?.message||'当前意图没有可用方案')}</div>`}
  function redesignRenderV10(){if(!v10InspectorPanel?.classList.contains('open'))return;const node=v10InspectorCurrentNode(),body=v10InspectorPanel.querySelector('.v10-inspector-body');if(!node){body.innerHTML='<div class="v10-inspector-empty">请先打开一个页面。</div>';return}const role=node.composer?.role||node.deckRole||ComposerV10Inspector.inferRole(node),dirty=ComposerV10Inspector.Provenance.isDirty(node),capabilities=ComposerV10Inspector.designIntentCapabilities(node),current=redesignIntentV10(node),resolution=redesignCandidatesV10(node,current);body.innerHTML=`<div class="v10-inspector-meta"><span class="v10-inspector-chip">${esc(ComposerV10Inspector.roleLabel(role))}</span><span class="v10-inspector-chip">${esc(node.composer?.selectedTemplateId||'手工页面')}</span>${dirty?'<span class="v10-inspector-chip dirty">人工修改已保护</span>':''}</div>${dirty?'<div class="v10-inspector-warning">当前页已有人工修改。只有点击某个方案时才会覆盖当前自动生成布局；修改下方意图本身不会立刻改页面。</div>':''}<p class="v10-inspector-note">先描述你想要的效果，再从 A / B / C 中选择最终结构。设计意图不再是第二套独立模板入口。</p><div class="v10-inspector-controls">${capabilities.length?capabilities.map(item=>v10InspectorControlHtml(item,current,node)).join(''):'<div class="v10-inspector-empty">当前页面使用自动设计。</div>'}</div><div style="margin-top:14px;padding-top:14px;border-top:1px solid rgba(128,138,160,.14)"><div style="font-size:12px;font-weight:800;margin-bottom:9px">页面方案</div><div class="v99-smart-template-grid">${redesignCandidateHtmlV10(resolution)}</div></div><div class="v10-inspector-actions"><button type="button" id="v10InspectorReset">恢复自动</button><button type="button" class="primary" id="v10InspectorDone">完成</button></div>`;
    body.querySelectorAll('[data-intent-field]').forEach(control=>control.addEventListener('change',()=>{const field=control.dataset.intentField,next={...current},value=v10InspectorReadField(control,field);if(value===undefined)delete next[field];else next[field]=value;const check=ComposerV10Inspector.validateDesignIntent(next,{content:node.composer?.content||{}});if(!check.ok){toast('设计意图无效：'+check.errors[0].message);return}redesignDraftV10.set(node.id,check.intent);redesignRenderV10()}));
    body.querySelectorAll('[data-redesign-template]').forEach(button=>button.onclick=()=>{const selected=(resolution.candidates||[]).find(item=>item.templateId===button.dataset.redesignTemplate);if(!selected)return;const isDirty=ComposerV10Inspector.Provenance.isDirty(node);if(isDirty&&!confirm('当前页已有人工编辑。应用该方案会重新生成布局，是否继续？'))return;checkpoint();try{const result=ComposerV10Inspector.applyTemplate({node,templateId:selected.templateId,params:selected.params,theme:node.deckTheme||data.deckTheme,density:current.density||node.deckDensity||data.deckDensity,role:resolution.role,force:isDirty,strictParams:true,intent:current});node.composer.designIntent={...current};node.composer.intentResolution={matcher:'Core.Composer.matchTemplates',templateId:selected.templateId,params:{...(selected.params||{})},candidateCount:(resolution.candidates||[]).length};normalize();save();renderEditor();redesignDraftV10.delete(node.id);redesignRenderV10();toast(`已应用${button.querySelector('.rank')?.textContent||'页面方案'}`);return result}catch(err){console.error(err);toast('重新设计失败：'+(err?.message||err))}});
    body.querySelector('#v10InspectorReset').onclick=()=>{redesignDraftV10.set(node.id,{});redesignRenderV10()};body.querySelector('#v10InspectorDone').onclick=v10InspectorClose;
  }
  function openRedesignV10(){const node=v10InspectorCurrentNode();if(!node){toast('请先进入某个页面的自由画布编辑');return}redesignDraftV10.set(node.id,ComposerV10Inspector.readDesignIntent(node));v10InspectorPanel.classList.add('open');redesignRenderV10()}
  v10InspectorRender=redesignRenderV10;
  if(v10InspectorButton){v10InspectorButton.textContent='重新设计本页';v10InspectorButton.title='设置设计意图并从 A/B/C 页面方案中选择';v10InspectorButton.onclick=openRedesignV10}
  const redesignHead=v10InspectorPanel?.querySelector('.v10-inspector-head');if(redesignHead){redesignHead.querySelector('h3').textContent='重新设计本页';redesignHead.querySelector('p').textContent='设计意图是约束，A/B/C 是最终模板结果。'}
  if(smartPageButton){smartPageButton.textContent='重新设计本页';smartPageButton.onclick=openRedesignV10}
  if(smartMobileButton)smartMobileButton.title='重新设计本页';
