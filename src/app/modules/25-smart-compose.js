  // V9.9 Smart Deck UI host. All composition rules live in the single Shared Runtime Composer.
  const ComposerV99=Core.Composer;
  const SMART_SAMPLE=`# 项目复盘：从问题到结果
> 面向团队的一次 10 分钟复盘

## 为什么要做
- 现状是什么
- 真正的问题在哪里
- 不处理会有什么影响

## 核心指标
- 32% 转化率提升
- 4.8x 处理效率
- 12 周完成交付

## 推进流程
- 明确问题
- 形成方案
- 小步验证
- 交付复盘

## 方案对比
- 原方案：功能多但路径长
- 新方案：聚焦关键动作

## 结论与下一步
- 先解决真实问题，再增加功能
- 负责人和验证标准必须明确
- 两周后复盘结果`;
  let smartOverlay=null,smartComposeButton=null,smartPageButton=null,smartMobileButton=null;
  function installSmartStyles(){if(document.getElementById('minddeck-v99-smart-styles'))return;const style=document.createElement('style');style.id='minddeck-v99-smart-styles';style.textContent=`
      .v99-smart-entry{position:relative}.v99-smart-entry::after{content:'9.9';position:absolute;right:-4px;top:-7px;font-size:9px;font-weight:800;padding:1px 4px;border-radius:8px;background:var(--accent,#5b6cff);color:#fff}
      .v99-smart-overlay{position:fixed;inset:0;z-index:12000;background:rgba(11,16,28,.58);backdrop-filter:blur(8px);display:flex;align-items:center;justify-content:center;padding:24px}.v99-smart-dialog{width:min(1040px,96vw);max-height:min(840px,92vh);overflow:auto;background:var(--panel,#fff);color:var(--text,#1f2329);border:1px solid rgba(128,138,160,.24);border-radius:28px;box-shadow:0 30px 90px rgba(0,0,0,.28)}
      .v99-smart-head{display:flex;gap:16px;align-items:flex-start;padding:24px 26px 18px;border-bottom:1px solid rgba(128,138,160,.18)}.v99-smart-head h2{margin:0 0 5px;font-size:22px}.v99-smart-head p{margin:0;color:var(--muted,#737b8b);font-size:13px;line-height:1.6}.v99-smart-head .spacer{flex:1}.v99-smart-close{width:36px;height:36px;border-radius:12px;border:0;background:rgba(128,138,160,.12);color:inherit;cursor:pointer;font-size:20px}
      .v99-smart-body{padding:22px 26px 26px}.v99-smart-grid{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(300px,.65fr);gap:20px}.v99-smart-card{border:1px solid rgba(128,138,160,.2);border-radius:20px;padding:18px;background:rgba(128,138,160,.045)}.v99-smart-card h3{font-size:14px;margin:0 0 12px}
      .v99-smart-card textarea{width:100%;height:370px;box-sizing:border-box;resize:vertical;border-radius:14px;border:1px solid rgba(128,138,160,.28);padding:14px 15px;background:var(--input,#fff);color:inherit;font:14px/1.65 ui-monospace,SFMono-Regular,Consolas,monospace;outline:none}.v99-smart-card textarea:focus,.v99-smart-card select:focus{border-color:#6374ff;box-shadow:0 0 0 3px rgba(99,116,255,.12)}
      .v99-smart-row{display:grid;grid-template-columns:1fr 1fr;gap:12px}.v99-smart-field{margin-bottom:13px}.v99-smart-field label{display:block;font-size:12px;font-weight:700;margin:0 0 6px;color:var(--muted,#737b8b)}.v99-smart-field select{width:100%;height:40px;border-radius:11px;border:1px solid rgba(128,138,160,.25);background:var(--input,#fff);color:inherit;padding:0 10px}.v99-smart-preview{min-height:110px;border-radius:14px;padding:13px;background:rgba(99,116,255,.07);border:1px solid rgba(99,116,255,.15);font-size:12px;line-height:1.55}.v99-smart-preview b{display:block;font-size:14px;margin-bottom:6px}.v99-smart-tags{display:flex;gap:6px;flex-wrap:wrap;margin-top:9px}.v99-smart-tag{padding:4px 7px;border-radius:999px;background:rgba(128,138,160,.13);font-size:11px}
      .v99-smart-note{font-size:11px;line-height:1.55;color:var(--muted,#737b8b);margin-top:10px}.v99-smart-warning{padding:11px 12px;border-radius:12px;background:rgba(216,95,69,.11);border:1px solid rgba(216,95,69,.25);font-size:12px;line-height:1.55;margin-bottom:12px}.v99-smart-ok{padding:9px 11px;border-radius:11px;background:rgba(28,156,109,.10);font-size:12px;margin-bottom:12px}.v99-smart-actions{display:flex;gap:10px;justify-content:flex-end;margin-top:18px;flex-wrap:wrap}.v99-smart-actions button{min-height:40px;padding:0 16px;border-radius:12px;border:1px solid rgba(128,138,160,.25);background:transparent;color:inherit;font-weight:700;cursor:pointer}.v99-smart-actions .primary{background:#5b6cff;border-color:#5b6cff;color:#fff}
      .v99-smart-template-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.v99-smart-template{min-height:150px;padding:14px;border-radius:15px;border:1px solid rgba(128,138,160,.22);background:rgba(128,138,160,.04);cursor:pointer;text-align:left;color:inherit}.v99-smart-template.active{border-color:#5b6cff;background:rgba(91,108,255,.12);box-shadow:inset 0 0 0 1px #5b6cff}.v99-smart-template .rank{font-size:11px;font-weight:900;color:#5b6cff}.v99-smart-template b{display:block;margin:6px 0;font-size:14px}.v99-smart-template small{display:block;color:var(--muted,#737b8b);line-height:1.5}.v99-smart-template code{display:block;margin-top:8px;font-size:10px;color:var(--muted,#737b8b);white-space:normal;word-break:break-all}.v99-smart-mobile{display:none;position:fixed;right:16px;bottom:82px;z-index:3900;width:48px;height:48px;border:0;border-radius:50%;background:#5b6cff;color:white;box-shadow:0 10px 28px rgba(39,50,120,.35);font-weight:900}
      @media(max-width:820px){.v99-smart-overlay{padding:8px;align-items:flex-end}.v99-smart-dialog{width:100%;max-height:92vh;border-radius:24px 24px 0 0}.v99-smart-grid{grid-template-columns:1fr}.v99-smart-card textarea{height:260px}.v99-smart-row{grid-template-columns:1fr}.v99-smart-template-grid{grid-template-columns:1fr}.v99-smart-mobile{display:block}.desktop-project-tools .v99-smart-entry{display:none}}
    `;document.head.appendChild(style)}
  function optionHtml(items,selected){return items.map(item=>`<option value="${esc(item.id)}" ${item.id===selected?'selected':''}>${esc(item.label)}</option>`).join('')}function densityOptions(selected='standard'){return [['compact','精简'],['standard','标准'],['rich','丰富']].map(([id,label])=>`<option value="${id}" ${id===selected?'selected':''}>${label}</option>`).join('')}function layoutOptions(selected='balanced'){return [['balanced','左右展开'],['right','向右展开'],['left','向左展开'],['down','向下树状'],['radial','自由放射']].map(([id,label])=>`<option value="${id}" ${id===selected?'selected':''}>${label}</option>`).join('')}function removeSmartOverlay(){smartOverlay?.remove();smartOverlay=null}
  function makeOverlay(title,subtitle){removeSmartOverlay();const overlay=document.createElement('div');overlay.className='v99-smart-overlay';const dialog=document.createElement('div');dialog.className='v99-smart-dialog';const head=document.createElement('div');head.className='v99-smart-head';const copy=document.createElement('div');copy.innerHTML=`<h2>${esc(title)}</h2><p>${esc(subtitle)}</p>`;const spacer=document.createElement('div');spacer.className='spacer';const close=document.createElement('button');close.className='v99-smart-close';close.type='button';close.textContent='×';close.onclick=removeSmartOverlay;head.append(copy,spacer,close);dialog.appendChild(head);overlay.appendChild(dialog);document.body.appendChild(overlay);overlay.addEventListener('pointerdown',event=>{if(event.target===overlay)removeSmartOverlay()});smartOverlay=overlay;return dialog}
  function sourcePreview(source){try{return ComposerV99.parseOutline(source)}catch(err){return {title:'无法解析',subtitle:String(err?.message||err),pageCount:0,specs:[]}}}function collectPreviewRoles(parsed){const roles={cover:1};const visit=(spec,depth=1)=>{const pseudo={title:spec.title,text:spec.text,points:spec.points||[],children:spec.children||[]},role=ComposerV99.inferRole(pseudo,{depth,root:false,preserveExplicit:false});roles[role]=(roles[role]||0)+1;(spec.children||[]).forEach(child=>visit(child,depth+1))};(parsed.specs||[]).forEach(spec=>visit(spec));return roles}function renderComposePreview(box,source){const parsed=sourcePreview(source),roles=collectPreviewRoles(parsed),roleLabels=Object.entries(roles).map(([role,count])=>`<span class="v99-smart-tag">${esc(ComposerV99.roleLabel(role))} × ${count}</span>`).join('');box.innerHTML=`<b>${esc(parsed.title||'未命名演示')}</b><div>${parsed.pageCount||0} 页 · 24 个结构模板由 Matcher + Diversity Allocator 自动分配</div>${parsed.subtitle?`<div style="margin-top:4px">${esc(parsed.subtitle)}</div>`:''}<div class="v99-smart-tags">${roleLabels}</div>`}
  function openSmartComposer(){const dialog=makeOverlay('智能组稿','大纲先标准化为 SlideContent，再由 Shared Runtime 的 Matcher、Capacity 与 Diversity Allocator 生成 MindDeck 原生 slideElements。'),body=document.createElement('div');body.className='v99-smart-body';body.innerHTML=`<div class="v99-smart-grid"><div class="v99-smart-card"><h3>1. 粘贴 Markdown / 大纲</h3><textarea id="v99Outline" spellcheck="false"></textarea><div class="v99-smart-note">推荐：# 标题、## 章节、### 子页、- 要点。生成前自动建立恢复备份。</div></div><div class="v99-smart-card"><h3>2. 整套设计策略</h3><div class="v99-smart-field"><label>演示主题</label><select id="v99Theme">${optionHtml(ComposerV99.themes,data.deckTheme||'aurora')}</select></div><div class="v99-smart-row"><div class="v99-smart-field"><label>信息密度</label><select id="v99Density">${densityOptions(data.deckDensity||'standard')}</select></div><div class="v99-smart-field"><label>导图布局</label><select id="v99Layout">${layoutOptions(data.mapLayout||'balanced')}</select></div></div><div class="v99-smart-field"><label>生成预览</label><div class="v99-smart-preview" id="v99Preview"></div></div><div class="v99-smart-note">仍使用现有 Editor / PresentationView / Portable Runtime；Composer 不拥有第二套渲染器。</div></div></div><div class="v99-smart-actions"><button type="button" id="v99SampleBtn">载入示例</button><button type="button" id="v99CancelBtn">取消</button><button type="button" class="primary" id="v99GenerateBtn">生成整套演示</button></div>`;dialog.appendChild(body);const source=body.querySelector('#v99Outline'),preview=body.querySelector('#v99Preview');let previewTimer=null;const refresh=()=>{clearTimeout(previewTimer);previewTimer=setTimeout(()=>renderComposePreview(preview,source.value),80)};source.addEventListener('input',refresh);renderComposePreview(preview,'');body.querySelector('#v99SampleBtn').onclick=()=>{source.value=SMART_SAMPLE;renderComposePreview(preview,source.value)};body.querySelector('#v99CancelBtn').onclick=removeSmartOverlay;body.querySelector('#v99GenerateBtn').onclick=()=>{const raw=source.value.trim();if(!raw){toast('请先粘贴大纲，或载入示例');source.focus();return}const parsed=sourcePreview(raw);if(!parsed.pageCount){toast('大纲无法解析');return}if(!confirm(`将根据大纲生成 ${parsed.pageCount} 页，并替换当前项目。当前项目会先建立恢复备份，是否继续？`))return;checkpoint();createRecoveryBackup('before-v9.9-smart-compose');try{const next=ComposerV99.compose(raw,{theme:body.querySelector('#v99Theme').value,density:body.querySelector('#v99Density').value,mapLayout:body.querySelector('#v99Layout').value,uiTheme:data.uiTheme||'light'}),quality=ComposerV99.Quality.validateProject(next);if(!quality.ok)throw new Error(quality.errors[0]?.message||'Composer Quality Validator 未通过');data=next;normalize();selectedNodeId=data.id;applyUiTheme();syncMapLayoutControls();save();renderMap();renderOrderPanel();fitAll();updateMobileNodeContext();removeSmartOverlay();toast(`已生成 ${ComposerV99.describe(data).pages} 页 · 使用 ${quality.metrics.templateCount} 个模板`)}catch(err){console.error(err);toast('智能组稿失败：'+(err?.message||err))}}}
  function renderTemplateRecommendations(grid,candidates,selectedTemplateId){grid.innerHTML='';const labels=['A 推荐','B 备选','C 备选'];candidates.forEach((candidate,index)=>{const button=document.createElement('button');button.type='button';button.className='v99-smart-template'+(candidate.templateId===selectedTemplateId?' active':'');button.dataset.template=candidate.templateId;button.innerHTML=`<span class="rank">${labels[index]||`方案 ${index+1}`}</span><b>${esc(candidate.label)}</b><small>${esc(candidate.reasons.join(' · '))}</small><code>${esc(candidate.templateId)} · ${esc(candidate.family)}</code>`;grid.appendChild(button)});if(!candidates.length)grid.innerHTML='<div class="v99-smart-warning">当前内容超出该角色可用模板的 Capacity，请缩减内容或选择其他页面角色。</div>'}
  function openPageDesigner(){if(!editorOpen||editorMode!=='slide'){toast('请先进入某个页面的自由画布编辑');return}const node=findNode(editorNodeId);if(!node){toast('当前页面不存在');return}let selectedRole=node.composer?.role||node.deckRole||ComposerV99.inferRole(node,{root:node.id===data.id}),candidates=[],selectedTemplateId=null;const dirty=ComposerV99.Provenance.isDirty(node),dialog=makeOverlay('页面方案 A / B / C','推荐的是同一内容的不同结构模板，不再只是切换页面 Role。人工编辑过的页面会被 Provenance 保护。'),body=document.createElement('div');body.className='v99-smart-body';body.innerHTML=`${dirty?'<div class="v99-smart-warning" id="v99DirtyWarning"><b>检测到人工编辑</b><br>当前 slideElements 已不同于 generatedHash。切换版式会先要求确认；整套换主题默认跳过此页。</div>':'<div class="v99-smart-ok">当前页仍与 Composer 生成版本一致，可安全重新套版。</div>'}<div class="v99-smart-card"><div class="v99-smart-row"><div class="v99-smart-field"><label>页面角色</label><select id="v99PageRole">${optionHtml(ComposerV99.roles,selectedRole)}</select></div><div class="v99-smart-field"><label>信息密度</label><select id="v99PageDensity">${densityOptions(node.deckDensity||data.deckDensity||'standard')}</select></div></div><div class="v99-smart-field"><label>演示主题</label><select id="v99PageTheme">${optionHtml(ComposerV99.themes,node.deckTheme||data.deckTheme||'aurora')}</select></div><h3>推荐结构</h3><div class="v99-smart-template-grid" id="v99TemplateGrid"></div><div class="v99-smart-note">当前模板：${esc(node.composer?.selectedTemplateId||'手工页面 / 未记录')}</div></div><div class="v99-smart-actions"><button id="v99RethemeAll" type="button">整套换主题</button><button id="v99PageCancel" type="button">取消</button><button class="primary" id="v99ApplyTemplate" type="button">应用所选方案</button></div>`;dialog.appendChild(body);const grid=body.querySelector('#v99TemplateGrid'),roleSelect=body.querySelector('#v99PageRole'),refresh=()=>{selectedRole=roleSelect.value;candidates=ComposerV99.recommendNodeTemplates(node,{role:selectedRole,limit:3});selectedTemplateId=candidates[0]?.templateId||null;renderTemplateRecommendations(grid,candidates,selectedTemplateId);grid.querySelectorAll('.v99-smart-template').forEach(button=>button.onclick=()=>{selectedTemplateId=button.dataset.template;grid.querySelectorAll('.v99-smart-template').forEach(item=>item.classList.toggle('active',item.dataset.template===selectedTemplateId))})};roleSelect.onchange=refresh;refresh();body.querySelector('#v99PageCancel').onclick=removeSmartOverlay;body.querySelector('#v99ApplyTemplate').onclick=()=>{if(!selectedTemplateId){toast('当前内容没有可用模板');return}const isDirty=ComposerV99.Provenance.isDirty(node);if(isDirty&&!confirm('当前页已有人工编辑。应用新方案会覆盖当前页 slideElements，但不会影响页面内容。确认继续？'))return;checkpoint();try{ComposerV99.applyTemplate({node,templateId:selectedTemplateId,role:selectedRole,theme:body.querySelector('#v99PageTheme').value,density:body.querySelector('#v99PageDensity').value,force:isDirty});normalize();save();renderEditor();removeSmartOverlay();toast(`已应用 ${ComposerV99.TemplateRegistry.get(selectedTemplateId)?.label||selectedTemplateId}`)}catch(err){toast('套版失败：'+(err?.message||err))}};body.querySelector('#v99RethemeAll').onclick=()=>{const theme=body.querySelector('#v99PageTheme').value;if(!confirm('整套切换主题会重绘未被人工修改的 Composer 页面；dirty 页面会自动跳过。继续？'))return;checkpoint();ComposerV99.rethemeProject(data,theme,{regenerate:true,protectDirty:true});data.deckTheme=theme;normalize();save();renderEditor();renderMap();const skipped=(data.composerWarnings||[]).filter(item=>item.code==='COMPOSER_DIRTY').length;removeSmartOverlay();toast(skipped?`主题已更新，跳过 ${skipped} 个人工编辑页面`:'整套智能页面主题已更新')}}
  function initSmartComposerV99(){if(!ComposerV99||document.getElementById('v99SmartComposeBtn'))return;installSmartStyles();const host=document.querySelector('.desktop-project-tools');if(host){smartComposeButton=document.createElement('button');smartComposeButton.id='v99SmartComposeBtn';smartComposeButton.className='btn primary v99-smart-entry';smartComposeButton.textContent='智能组稿';smartComposeButton.title='从 Markdown / 大纲生成整套可编辑演示';smartComposeButton.onclick=openSmartComposer;host.prepend(smartComposeButton)}const editorTools=document.querySelector('.editor-topbar .element-toolbar');if(editorTools){smartPageButton=document.createElement('button');smartPageButton.id='v99PageDesignerBtn';smartPageButton.className='btn';smartPageButton.textContent='页面方案';smartPageButton.title='获取真正的 A/B/C 结构模板推荐';smartPageButton.onclick=openPageDesigner;editorTools.appendChild(smartPageButton)}smartMobileButton=document.createElement('button');smartMobileButton.className='v99-smart-mobile';smartMobileButton.id='v99SmartMobileBtn';smartMobileButton.textContent='组';smartMobileButton.title='智能组稿';smartMobileButton.onclick=openSmartComposer;document.body.appendChild(smartMobileButton)}
  setTimeout(initSmartComposerV99,0);