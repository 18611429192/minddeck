# V10 Golden DeckSpec Samples

These files are representative DeckSpec v1 inputs used to freeze the V9.9 Composer behavior before V10 feature work begins.

They are ordinary Composer inputs, not a second Project format. Runtime output continues to be the native MindDeck Project / `slideElements`. Expected regression projections live under `tests/golden/snapshots/` and intentionally omit non-semantic random IDs.
