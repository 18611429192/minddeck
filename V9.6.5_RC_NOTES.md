# MindDeck V9.6.5 RC

这是 V9.6 统一架构阶段的收口版本。

本版原则：

> 单文件可以继续存在，但单文件不能再意味着维护第二份业务源码。

主要变化：

- Shared Runtime 成为浏览器应用、Node/CI、Portable HTML 的唯一业务源。
- 编辑思维导图和导出思维导图共用 MapRenderer / Layout / Commands。
- 编辑器演示和导出演示共用 PresentationSession / TocRenderer / Input / Fullscreen。
- 页面编辑、主程序演示、导出演示共用 Element / Slide / Animation。
- 母版和页面的最终合成规则只有 Slide 一份。
- 项目自检、恢复数据、历史容量限制也收回 Shared Runtime。
- Portable 模板不再内置第二套业务代码，只初始化 `MindDeckCore.Portable.mount(...)`。
- `src/core/*.js` 全部变为 Shared Runtime 的 thin adapter。
- 新增架构审计，防止后续再次分叉。

这版之后再完善演示功能时，应直接修改 Shared Presentation Runtime。
